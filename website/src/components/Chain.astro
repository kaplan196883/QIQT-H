---
import katex from 'katex';
// Render each label's math to HTML at build time (robust — no client-side dependency).
const m = (tex) => katex.renderToString(tex, { throwOnError: false });

// The argument in five links. Each link carries its honest status colour.
const links = [
  { n: '1 · Holography',
    t: `A bounded region ${m('R')} has finite capacity ${m('Q_R = A/4\\ell_P^2')}.`,
    cls: 's-postulate', tag: 'Postulate (FQ)' },
  { n: '2 · Regional cost',
    t: `Regional content is a state on the Type II algebra; cost = Araki relative entropy ${m('\\chi_R')}.`,
    cls: 's-verified', tag: 'Calculus verified' },
  { n: '3 · Decoherence',
    t: `Standard unitary decoherence makes the macroscopic records non-interfering and redundantly objective — but this removes interference, it does not make one record actual.`,
    cls: 's-verified', tag: 'Standard QM' },
  { n: '4 · Single record',
    t: `A non-dynamical selector λ makes exactly one record actual; Φ stays exactly unitary. ${m('Q_R')} bounds how many distinguishable records exist (the finite stage) — it does NOT forbid two being actual (that "capacity overflow" form is a category error). The single outcome is λ's, an Everett-like selection postulate.`,
    cls: 's-conditional', tag: 'Selection postulate (λ)' },
  { n: '5 · Born',
    t: `Probabilities ${m('|c_k|^2')} from typicality — machine-checked down to a no-signaling / reversibility postulate, with a no-go on anything weaker; not yet derived.`,
    cls: 's-open', tag: 'Open · reduced' },
];
---
<div class="chain">
  {links.map((l) => (
    <div class="link">
      <div class="n">{l.n}</div>
      <div class="t" set:html={l.t}></div>
      <span class={`pill ${l.cls}`}><span class="dot"></span>{l.tag}</span>
    </div>
  ))}
</div>
